import { ScreenStub } from '@/shared/ui/ScreenStub'

export function ItemNewPage() {
  return (
    <ScreenStub
      title="옷 등록"
      note="사진 → 타이틀·카테고리 → 선택 항목 순서로 구성될 화면입니다. 아직 구현 전이에요."
    />
  )
}
