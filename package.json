{
  "name": "mapsy",
  "private": true,
  "packageManager": "pnpm@10.18.2",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
    "preinstall": "node scripts/only-pnpm.mjs",
    "prepare": "git config core.hooksPath .githooks 2>/dev/null || exit 0",
    "setup:worktree": "bash scripts/setup-worktree.sh",
    "dev": "pnpm --filter mapsy-frontend dev",
    "preview": "pnpm --filter mapsy-frontend preview",
    "codegen": "pnpm --filter mapsy-frontend codegen",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:db": "bash supabase/tests/run.sh",
    "cf:deploy": "pnpm --filter mapsy-frontend cf:deploy",
    "cf:upload": "pnpm --filter mapsy-frontend cf:upload",
    "cf:dev": "pnpm --filter mapsy-frontend cf:dev",
    "types:gen": "supabase gen types typescript --linked > mapsy-frontend/src/shared/api/database.types.ts.tmp && mv mapsy-frontend/src/shared/api/database.types.ts.tmp mapsy-frontend/src/shared/api/database.types.ts"
  }
}
