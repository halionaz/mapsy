{
  "name": "mapsy",
  "private": true,
  "packageManager": "pnpm@10.18.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter mapsy-frontend dev",
    "preview": "pnpm --filter mapsy-frontend preview",
    "codegen": "pnpm --filter mapsy-frontend codegen",

    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  }
}
